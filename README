Filip Emil Florentin -  323CB
<filipemil02@yahoo.com>

Codul este putin cam dezordonat, pentru ca n-am putut face multe funcții dat fiind faptul ca atunci când m-am apucat de tema, încercam sa dau comenzi din terminal pentru a vedea dacă merge și aveam erori pentru ca nu recunoștea clase și parametri externi Tema1.java(users,intrebare,...)
Așa ca, în mare parte, tot codul este scris în Tema1, în main.In fiecare if verific primul argument pentru a știi ce e de făcut mai departe și ce lipsește.De asemenea, se afla un i la începutul fiecareui if ca sa văd atunci când am eroare(încerc sa accesez parametrul x al vectorului args iar acesta nu exista), urmând ca înaintea fiecărei apelari a vectorului, acesta sa fie incrementat pentru a putea afișa mesajul corespunzător utilizatorului.Am făcut 2 liste pentru users și întrebări pentru a avea acces mai ușor la anumite date.Fiecare parametru este pus într-un string și parsat cu funcția split după caracterul potrivit( ' ).
Pentru crearea de utilizator a fost suficient sa verific lungimea array-ului pentru ca nu putea primi și alți parametrii ca în cadrul celorlalte funcții.Functia adaugaUser verifica mai întâi dacă este deja adăugat un user cu username-ul respectiv.In caz afirmativ, aceasta întoarce 0, urmand sa se afisze mesajul corespunzător în acest caz și întoarce 1 în caz contrar, dar nu înainte ca acesta sa fie introduc în baza de date.
Pentru clean, se apelează o funcție ce deschide fiecare fișier în parte, de aceasta data cu modul append oprit, pentru a face curățenie și în același timp resetează fiecare identificator din clasa întrebare și chestionar pentru a o lua de la început dat fiind faptul ca se dorește o repornire.
La creare de întrebări, se verifica dacă cumva sunt mai mult de 5 răspunsuri și se iau parametrii pe rând cu verificările corepsunzatoare pentru a putea introduce doar întrebări valide în sistem ( 
User și parola bune,intrebarea cu același text nu se afla deja în baza de date , răspunsurile dispun de tot parametrii necesari, număr valid de răspunsuri adevărate).Pentru răspunsuri se mai face o verificare sa nu fie introduse 2 răspunsuri asemenea iar dacă totul este în regula, întrebarea este adăugată cu succes.
Toate tasa-urile de aici încolo verifica existenta parametrilor de login și corectitudinea acestora.Pentru id-ul întrebării și al chestionarelor sunt făcute funcții separate ce deschid fișierele corespunzătoare și parseaza conținutul după virgula.
Pentru Afișarea tuturor chestionarelor și a întrebărilor sunt făcute de asemenea funcții ce deschid fișierele corespunzătoare și afișează tot conținutul.
Pentru crearea unui quizz, se fac verificări asemănătoare ca la întrebări(existenta) și se crează o lista cu întrebările doar în cazul în care fiecare întrebare cu id-ul corespunzător exista deja în fișier.
Pentru Afișarea id-ului quizului se parcurge fișierul cu quiz-uri și se întoarce cu ajutorul funcției split identificatorul.
Celelalte funcții doar verifica datele de log în ale utilizatorului .